<%= form_for(guestbook) do |f| %>
  <% if guestbook.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(guestbook.errors.count, "error") %> prohibited this guestbook from being saved:</h2>

      <ul>
      <% guestbook.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :message %>
    <%= f.text_field :message %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
